# policy.yaml
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: rss-api
  namespace: dev
spec:
  imageRepositoryRef:
    name: rss-api
  filterTags:
    pattern: '^build-(?P<num>[0-9]+)$'
    extract: '$num'
  policy:
    numerical:
      order: asc
